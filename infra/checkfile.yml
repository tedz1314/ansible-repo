---
 - hosts: all
   name: Install and configure apache
   vars:
     local_source: /ansible/templates/"{{jdk_archive}}"
     local_source2: /software/"{{jdk_archive}}"
     local_source3: /software/abcdfile

   tasks:
   - name: check if file source exists
     stat:
       path: "{{ local_source }}"
   - name: check if file source exists
     stat:
       path: "{{ local_source2 }}"
   - name: check if file source exists
     stat:
       path: "{{ local_source3 }}"
